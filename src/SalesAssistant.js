import './SalesAssistant.css';


/**
 * SalesAssistant
 *
 * Controls which page is displayed in QBuilder
 *
 * props:
 *
 * state:
 * isListening: t/f, is the mic recording
 * questions: [], questions for sales assistant
 * solution: {solution: {timeline, cost, details}
 *
 * SalesAssistant => {StartPage, ListeningPage, QuestionsPage, SolutionPage}
 */
function SalesAssistant() {
  return (
    <div className="SalesAssistant">
    </div>
  );
}

export default SalesAssistant;